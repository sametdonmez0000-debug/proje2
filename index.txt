<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Canlı Pixel Boyama Alanı</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #111827;
      color: white;
      text-align: center;
    }

    header {
      padding: 20px;
      background: #1f2937;
    }

    h1 {
      margin: 0;
      font-size: 26px;
    }

    .panel {
      margin: 20px auto;
      background: #1f2937;
      padding: 15px;
      width: 90%;
      max-width: 600px;
      border-radius: 12px;
    }

    input[type="color"] {
      width: 80px;
      height: 45px;
      border: none;
      cursor: pointer;
      border-radius: 8px;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(40, 18px);
      grid-template-rows: repeat(40, 18px);
      gap: 1px;
      justify-content: center;
      margin: 20px auto;
      touch-action: none;
    }

    .pixel {
      width: 18px;
      height: 18px;
      background: white;
      border: 1px solid #d1d5db;
      cursor: pointer;
    }

    .pixel:hover {
      transform: scale(1.15);
      border-color: #facc15;
    }

    .info {
      font-size: 14px;
      color: #d1d5db;
      margin-bottom: 20px;
    }

    @media (max-width: 800px) {
      .grid {
        grid-template-columns: repeat(40, 9px);
        grid-template-rows: repeat(40, 9px);
      }

      .pixel {
        width: 9px;
        height: 9px;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>Canlı Pixel Boyama Alanı</h1>
    <p>Rengini seç, piksellere dokun ve alanı birlikte doldur.</p>
  </header>

  <div class="panel">
    <label>Renk Seç:</label><br><br>
    <input type="color" id="colorPicker" value="#ff0000">
  </div>

  <div class="info">
    Toplam alan: 40 x 40 = 1600 piksel
  </div>

  <div class="grid" id="pixelGrid"></div>

 <script type="module">
  // Import the functions you need from the SDKs you need
  import { initializeApp } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js";
  import { getAnalytics } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-analytics.js";
  // TODO: Add SDKs for Firebase products that you want to use
  // https://firebase.google.com/docs/web/setup#available-libraries

  // Your web app's Firebase configuration
  // For Firebase JS SDK v7.20.0 and later, measurementId is optional
  const firebaseConfig = {
    apiKey: "AIzaSyBOQeWgxafFQVjUbfSw10MWccttXu9TFLY",
    authDomain: "pixel-99f00.firebaseapp.com",
    projectId: "pixel-99f00",
    storageBucket: "pixel-99f00.firebasestorage.app",
    messagingSenderId: "894234692943",
    appId: "1:894234692943:web:c74c9ad3eb6c7ed252c93a",
    measurementId: "G-CQ9K97ZT7C"
  };

  // Initialize Firebase
  const app = initializeApp(firebaseConfig);
  const analytics = getAnalytics(app);
</script>;

    const app = initializeApp(firebaseConfig);
    const db = getDatabase(app);

    const grid = document.getElementById("pixelGrid");
    const colorPicker = document.getElementById("colorPicker");

    const gridSize = 40;
    const totalPixels = gridSize * gridSize;

    // Pikselleri oluştur
    for (let i = 0; i < totalPixels; i++) {
      const pixel = document.createElement("div");
      pixel.classList.add("pixel");
      pixel.dataset.id = i;

      pixel.addEventListener("click", () => {
        const selectedColor = colorPicker.value;
        set(ref(db, "pixels/" + i), selectedColor);
      });

      grid.appendChild(pixel);
    }

    // Firebase'den canlı verileri oku
    const pixelsRef = ref(db, "pixels");

    onValue(pixelsRef, (snapshot) => {
      const data = snapshot.val();

      if (!data) return;

      Object.keys(data).forEach((id) => {
        const pixel = document.querySelector(`[data-id="${id}"]`);
        if (pixel) {
          pixel.style.backgroundColor = data[id];
        }
      });
    });
  </script>

</body>
</html>